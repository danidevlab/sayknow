name: Keep-Alive-Supabase
on:
  schedule:
    - cron: '30 15 * * *' # 한국 시간 00:30 (UTC 15:30)
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase Function
        run: curl "https://[본인프로젝트ID].supabase.co/functions/v1/[함수이름]?askdata=ping"
